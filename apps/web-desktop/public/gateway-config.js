// Docker replaces this file at container startup with its configured gateway.
// Keeping a checked-in empty default also makes local Vite development quiet.
window.__HERMES_GATEWAY_WHITELIST__ ??= []
